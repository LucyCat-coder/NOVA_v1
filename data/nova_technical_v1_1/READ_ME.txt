NOVA — набор для пробного дообучения, ограниченная проверка

Всего: 1398; train: 1258; validation: 140.
Удалены только tech_804 и tech_824: известные пересечения с bench_001 и bench_045.
Остальные записи, ответы и их метаданные не изменены. Разбиение сохранено.
Исходные файлы, benchmark и checkpoints не изменялись.

Состав:
nova_technical_v1_1_prepared.jsonl — весь подготовленный набор.
splits/train.jsonl — обучение; splits/val.jsonl — validation, не обучение.
loader/train — две копии исправленного UTF-8 загрузчика для проекта NOVA.
report.json — хеши, изменения и кандидаты на ошибки оформления (0 записей).

Это не сертификат полного качества. Другие смысловые пересечения, ошибки ответов
и обрывы могут оставаться. Автоматически ответы/код не переписывались.
Статус PASS внутри старых записей не означает новую независимую проверку.

Автору обучения:
1. Разместить новый набор в отдельной папке, не заменяя исходные данные.
2. Использовать TextDataset из train.dataset_utf8_fixed и соответствующий builder.
3. В отдельной конфигурации указать новые пути train/val, новое имя варианта
   и dataset_sha256 из report.json -> sha256_prepared -> full.
4. Назначить новую пустую out_dir; прежние checkpoints не перезаписывать.
5. Сначала проверить загрузку и предупреждения, затем согласовать пробный запуск.
6. Benchmark оставить прежним. Старые BASE/checkpoints уже могли видеть исключённые
   задачи: удаление примеров из нового набора не отменяет прежнее обучение.

Обучение этим скриптом не запускалось. Этот архив не содержит весов или окружения.
